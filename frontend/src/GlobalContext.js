// importing react component
import React from "react";

// creating a global context
const GlobalContext = React.createContext();

// exporting the global context to be used in App.js
export default GlobalContext;
